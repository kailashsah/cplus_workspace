#include <iostream>
using namespace std;

/*
	1. std::unique_lock l(m);
		c++17 allows template parameters to be deduced from constructors, removing the need for make_xxx(...) helper functions or explicitly specify typenames that the compiler should be able to deduce.
		this is called Template function argument deduction.


*/


